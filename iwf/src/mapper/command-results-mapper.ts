import { CommandResults } from "../command-results";
import { CommandResults as IdlCommandResults } from "../../../gen/iwfidl/api";

export class CommandResultsMapper {
    public static toCommandResults(idlCommandResults: IdlCommandResults): CommandResults {
        // TODO: add command results
        return {};
    }
}
