# Public repo: always resolve from the public npm registry so CI (GitHub-hosted
# runners) and external contributors can install without Indeed credentials.
# This also keeps package-lock.json from being repointed at a private mirror.
registry=https://registry.npmjs.org/
